<% filter = function(value){
	data.media.forEach(function(listing){ %>
		<li class="listing">
			<figure>
				<img src="<%= listing.poster %>">
				<h3><%= listing.title %></h3>
				<figcaption>
					Genres: <%= listing.genre.join(', ') %>
				</figcaption>
			</figure>
		</li>
	<% }) %>
<% } %>

